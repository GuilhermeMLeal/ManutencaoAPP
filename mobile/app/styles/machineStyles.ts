import { StyleSheet } from 'react-native';

const machineStyles = StyleSheet.create({
  cardMachine: {
    backgroundColor: 'white',
    padding: 16,
    marginBottom: 8,
    borderRadius: 8,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 1 },
    shadowOpacity: 0.3,
    shadowRadius: 1,
  },
});



export default machineStyles;
